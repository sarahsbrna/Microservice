spring.application.name=NOTIFICATION-SERVICE
server.port=8086

# --- KONFIGURASI EUREKA UNTUK DOCKER ---
# Arahkan ke container 'eureka', bukan 'localhost'
eureka.client.service-url.defaultZone=http://eureka:8761/eureka
# Daftarkan service ini dengan alamat IP-nya di jaringan Docker
eureka.instance.prefer-ip-address=true

# --- KONFIGURASI RABBITMQ UNTUK DOCKER ---
# Arahkan ke container 'my-rabbitmq', bukan 'localhost'
spring.rabbitmq.host=my-rabbitmq
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Properti custom untuk RabbitMQ (tidak berubah)
app.rabbitmq.exchange=order.exchange
app.rabbitmq.queue=order.notification.queue
app.rabbitmq.routingkey=order.created.key

# Konfigurasi Email (tidak berubah)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sarahalpenperdana@gmail.com
spring.mail.password=kkzrdwwplpydfxno
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

management.endpoints.web.exposure.include=*
# Memberitahu Actuator untuk menampilkan detail health
management.endpoint.health.show-details=always